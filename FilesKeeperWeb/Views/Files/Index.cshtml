@model IList<UserFile>
@*@{
            ViewData["Title"] = "Index";
    }*@



<h2>@ViewBag.Title</h2>

@*<div class="row">
        <div class="col-md-9">
            <div id="dropzone">
                <form action="/Files/Upload" class="dropzone needsclick dz-clickable" id="uploader">
                    <div class="dz-message needsclick">
                        Drop files here or click to upload.<br>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <form asp-action="AddFiles" asp-controller="Files" method="post" enctype="multipart/form-data">
            <input type="file" name="uploads" /><br>
            <input type="file" name="uploads" /><br>
            <input type="file" name="uploads" /><br>
            <input type="submit" value="Загрузить" />
        </form>*@

<p><a asp-area="" asp-controller="Files" asp-action="SendFiles" class="btn btn-default">Загрузить файлы</a></p>
@*asp-route-id="@item.Id"*@
@if (Model.Count > 0)
{
    <table class="table">
        <tr><td>Имя файла</td>@*<td>Производитель</td><td>Цена</td>*@</tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @item.Filename
                </td>
                <td>
                    <form asp-controller="Files" asp-action="LoadFile" method="post">
                        @*<div class="form-group">*@
                            <input type="hidden" name="id" value="@item.Id">
                            <input type="hidden" name="hash" value="@item.Hash">
                            <input type="submit" class="btn btn-default" value="Скачать" />
                        @*</div>*@
                    </form>
                </td>
                <td>
                    <form asp-controller="Files" asp-action="Delete" method="post">
                        @*<div class="form-group">*@
                        <input type="hidden" name="id" value="@item.Id">
                        <input type="hidden" name="hash" value="@item.Hash">
                        <input type="submit" class="btn btn-default" value="Удалить" />
                        @*</div>*@
                    </form>
                </td>
            </tr>
        }
    </table>
}


@*<script>
        $(document).ready(function () {
            Dropzone.options.uploader = {
                paramName: "file",
                maxFilesize: 50
            };
        });
    </script>
    <link href="~/css/dropzone.css" rel="stylesheet" />
    <script src="~/bundles/dropzone.js"></script>*@

